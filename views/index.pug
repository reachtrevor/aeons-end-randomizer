extends ./_shell

block Body
  ul.pages
    li.chat.page
      div.chat-area
        ul.messages
      input.input-message(placeholder="Type here...")
    li.login.page
      div.form
        h3.title What's your nickname?
        input.username-input(type="text", maxlength="14")


block Footer
  script(src="/socket.io/socket.io.js")
  script(src="main.js")
